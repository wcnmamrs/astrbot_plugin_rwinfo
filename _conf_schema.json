{
    "global_enabled": {
        "description": "全局解析开关",
        "type": "bool",
        "default": true
    },
    "mode": {
        "description": "名单模式: white(白名单) / black(黑名单) / mixed(混合模式@用户优先)",
        "type": "string",
        "default": "black",
        "options": ["white", "black", "mixed"]
    },
    "白名单": {
        "description": "白名单列表（black/white模式下使用）",
        "type": "list",
        "default": []
    },
    "黑名单": {
        "description": "黑名单列表（black/white模式下使用）",
        "type": "list",
        "default": []
    },
    "混合_白名单": {
        "description": "混合模式专用白名单（@用户/#群 独立管理）",
        "type": "list",
        "default": []
    },
    "混合_黑名单": {
        "description": "混合模式专用黑名单（@用户/#群 独立管理）",
        "type": "list",
        "default": []
    },
    "混合_私聊策略": {
        "description": "混合模式私聊默认策略: white(不在白名单拦截) / black(在黑名单拦截)",
        "type": "string",
        "default": "white",
        "options": ["white", "black"]
    },
    "私聊启用": {
        "description": "是否启用私聊查房处理（关闭则私聊不发房号）",
        "type": "bool",
        "default": true
    },
    "debug": {
        "description": "开启调试日志（输出详细运行信息）",
        "type": "bool",
        "default": false
    },
    "max_players_display": {
        "description": "玩家列表最多显示人数，超过则折叠",
        "type": "int",
        "default": 10
    },
    "show_players": {
        "description": "是否显示玩家列表（默认关闭，避免解析不稳定）",
        "type": "bool",
        "default": false
    },
    "retry_times": {
        "description": "探针名字被过滤时自动重试次数（0-10，默认3）",
        "type": "int",
        "default": 3,
        "min": 0,
        "max": 10
    },
    "auto_recall": {
        "description": "是否自动撤回房间解析信息/重试进度（默认关闭）",
        "type": "bool",
        "default": false
    },
    "recall_delay": {
        "description": "自动撤回延迟秒数（默认60秒，0=不撤回）",
        "type": "int",
        "default": 60,
        "min": 0,
        "max": 300
    }
}